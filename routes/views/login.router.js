const router = require('express').Router();
const React = require('react');
const ReactDOMServer = require('react-dom/server');
const LoginPage = require('../../views/LoginPage');

router.get('/', (req,res)=> {
  const loginPage = React.createElement()
})
